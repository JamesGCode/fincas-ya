"use client";import { useState } from "react";import { Star, X } from "lucide-react";import { Button } from "@/components/ui/button";import { Separator } from "@/components/ui/separator";import { useReviews } from "@/features/reviews/hooks/use-reviews";import { ReviewsList } from "./reviews-list";import { ReviewForm } from "./review-form";import {  Dialog,  DialogContent,  DialogHeader,  DialogTitle,  DialogTrigger,} from "@/components/ui/dialog";interface ReviewsSectionProps {  fincaId: string;}export function ReviewsSection({ fincaId }: ReviewsSectionProps) {  const { reviewCount, averageRating } = useReviews(fincaId);  const [isOpen, setIsOpen] = useState(false);  return (    <div className="py-10 max-md:px-3" id="reviews-section">      <Separator className="mb-10" />      <div className="flex flex-col md:flex-row items-center justify-between mb-8">        <div className="flex flex-col gap-1">          <h2 className="text-2xl font-bold">            Lo que dicen nuestros huéspedes          </h2>          <div className="flex items-center gap-1.5 text-base font-medium text-muted-foreground">            <span>{reviewCount} evaluaciones</span>          </div>        </div>        <Dialog open={isOpen} onOpenChange={setIsOpen}>          <DialogTrigger asChild>            <Button className="max-md:w-full max-md:mt-4 rounded-lg border-black bg-black text-white hover:text-white hover:bg-primary px-6">              Escribir una reseña            </Button>          </DialogTrigger>          <DialogContent className="sm:max-w-[600px] max-h-[90vh] flex flex-col p-0 gap-0 overflow-hidden rounded-xl">            <DialogHeader className="p-4 border-b flex flex-row items-center justify-center relative shrink-0">              {/* Close button is handled by DialogPrimitive usually, but we can add a custom one if needed or rely on default X */}              <DialogTitle className="text-base font-bold text-center">                Escribir una reseña              </DialogTitle>            </DialogHeader>            <div className="p-6 overflow-y-auto overscroll-contain">              <ReviewForm                fincaId={fincaId}                onReviewAdded={() => setIsOpen(false)}                onCancel={() => setIsOpen(false)}              />            </div>          </DialogContent>        </Dialog>      </div>      <ReviewsList fincaId={fincaId} />    </div>  );}