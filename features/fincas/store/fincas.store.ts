import { create } from "zustand";import { persist } from "zustand/middleware";interface PropertiesState {  search: string;  region: string;  currentPage: number;  itemsPerPage: number;  setSearch: (search: string) => void;  setRegion: (region: string) => void;  setCurrentPage: (page: number) => void;  setItemsPerPage: (itemsPerPage: number) => void;  resetFilters: () => void;}export const usePropertiesStore = create<PropertiesState>()(  persist(    (set) => ({      search: "",      region: "all",      currentPage: 1,      itemsPerPage: 10,      setSearch: (search) => set({ search, currentPage: 1 }), // Reset page on search change      setRegion: (region) => set({ region, currentPage: 1 }), // Reset page on region change      setCurrentPage: (currentPage) => set({ currentPage }),      setItemsPerPage: (itemsPerPage) => set({ itemsPerPage, currentPage: 1 }), // Reset page on limit change      resetFilters: () =>        set({ search: "", region: "all", currentPage: 1, itemsPerPage: 10 }),    }),    {      name: "properties-filters-storage", // name of the item in the storage (must be unique)    },  ),);