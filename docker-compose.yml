services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/factoryia/fincasyaweb:latest
    container_name: fincas-ya-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
